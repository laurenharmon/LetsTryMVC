@using LetsTryMVC.Models
@model FriendsList

<table class="table">
    <tr>
        <th scope="col">Name</th>
        <th></th>
    </tr>
    @foreach (var user in Model.Friends)
    {
        <tr>
            <td>@user.UserName</td>
        </tr>

    }
</table>
