@model List<Friends>


<h1>Your Friends:</h1>

<table class="table">
    <tr>
        <th scope="col">Name</th>

    </tr>
    @foreach (var friend in Model)
    {
        <tr>
            <td>@friend.Friend.UserName</td>
        </tr>
    }
    </table>

