@model List<Microsoft.AspNetCore.Identity.IdentityUser>


<table class="table">
    <tr>
        <th scope="col">Name</th>
        <th></th>
    </tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.UserName</td>
            <td>@Html.ActionLink("Add Friend", "AddFriend", "FriendsList", new { id = user.Id }, new { @class = "btn btn-dark" })</td>
        </tr>

    }
</table>
