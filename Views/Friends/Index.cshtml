@model List<Microsoft.AspNetCore.Identity.IdentityUser>


<table class="table">
    <tr>
        <th scope="col">Name</th>
        <th></th>
    </tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.UserName</td>
            @*<td>@Html.ActionLink("Add to cart", "AddToCart", "ShoppingCart", new { id = product.Id }, new { @class = "btn btn-info" })</td>*@
        </tr>

    }
</table>
